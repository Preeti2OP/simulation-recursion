#include<stdio.h>
long int fact(int fact);
int main()
{
int n,f;
printf("enter any no\n");
scanf("%d",&n);
f=fact(n);
printf("factorial=%d",f);
return 0;
}
long int fact(int f)
{
if (f==0)
return 1;
else if (f==0)
return 1;
else
return f*(f-1);
}
